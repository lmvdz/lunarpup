# Upstream refresh duplicate — archived

This note preserves the intent of the former duplicate concern `13-upstream-refresh.md`.

That file described refreshing the integration branch against upstream after the panel redesign. Its scope duplicated `13-upstream-sync.md`, which is now the single executable concern for convergence. No implementation work was discarded; the blocker, architecture decision, and verification requirements live in concern 13.

Archived during the premium-experience plan upgrade so dependency tooling has one unambiguous sibling concern numbered 13.
